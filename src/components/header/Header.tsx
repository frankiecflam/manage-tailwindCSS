import HeaderNav from "./HeaderNav";

const Header = () => {
  return (
    <header className="py-5 px-8 absolute top-0 left-0 w-full h-auto z-50 bg-transparent">
      <HeaderNav />
    </header>
  );
};

export default Header;
